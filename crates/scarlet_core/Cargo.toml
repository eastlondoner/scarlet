[package]
name = "scarlet_core"
version = "0.1.0"
edition = "2024"


# `cargo mordant` sets `--cfg mordant` and plain cargo does not, so
# the `#[cfg_attr(mordant, ...)]` suppression on `Compiler` is an
# unexpected cfg to every other build — and `clippy -- -D warnings` fails on it.
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(mordant)'] }

[dependencies]
scarlet_syntax = { path = "../scarlet_syntax" }
scarlet_ir = { path = "../scarlet_ir" }
scarlet_types = { path = "../scarlet_types" }
indexmap = { workspace = true }
smallvec = "1.15"
petgraph = "0.8"
include_dir = { version = "0.7", features = ["glob"] }
percent-encoding = "2"
