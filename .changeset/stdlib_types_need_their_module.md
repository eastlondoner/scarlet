---
default: major
---

A stdlib type is named with its module, like `map.Map(String, Int)`: the stdlib no longer puts every type's bare name in scope. The error for a bare `Map` says how to spell it.
