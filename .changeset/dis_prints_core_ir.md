---
default: minor
---

`scarlet dis` prints a program's Core IR, the typed form the compiler hands the VM, with the names of the types its constructors belong to. `--fn <name>` lists every function whose name contains it, from any module.
